<UserControl
    x:Class="ImageConverter.View.ConverterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:ImageConverter.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:ImageConverter.ViewModel"
    d:DataContext="{d:DesignInstance Type={x:Type vm:ConverterViewModel}}"
    d:DesignHeight="520"
    d:DesignWidth="800"
    DataContext="{Binding ConverterViewModel, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:Button
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Command="{Binding LoadImageBtnClickCommand}"
                Content="Upload Files" />
            <ui:Button
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Command="{Binding ClearBtnClickCommand}"
                Content="Clear" />
        </Grid>

        <Grid Grid.Row="2" AllowDrop="True">
            <ListBox ItemsSource="{Binding Images}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Drop">
                        <i:InvokeCommandAction Command="{Binding FileDropCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ListBox>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <CheckBox
                VerticalAlignment="Center"
                Content="Save To Directory Path"
                IsChecked="{Binding IsSaveDirectory, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
            <ui:TextBox
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                IsReadOnly="True"
                PlaceholderText="Directory Path"
                Text="{Binding SaveDirectoryPath}" />
            <ui:Button
                Grid.Column="3"
                VerticalAlignment="Stretch"
                Command="{Binding OpenDirectoryExplorerBtnClickCommand}"
                Content="..." />
        </Grid>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ComboBox VerticalAlignment="Stretch" VerticalContentAlignment="Stretch">
                <ComboBoxItem Content="JPG" />
                <ComboBoxItem Content="PNG" />
                <ComboBoxItem Content="BMP" />
            </ComboBox>

            <ui:Button
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Command="{Binding ConvertBtnClickCommand}"
                Content="Convert" />
        </Grid>

    </Grid>
</UserControl>
